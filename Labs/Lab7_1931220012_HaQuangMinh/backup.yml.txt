- name: Backup running-config
  hosts: ios
  gather_facts: no
  tasks:
    - name: Backup config
      cisco.ios.ios_config:
        backup: true
        backup_options:
          dir_path: backups
